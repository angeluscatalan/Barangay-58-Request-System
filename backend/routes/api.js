const express = require('express');
const router = express.Router();

// This file is for general API routes that don't belong to specific feature modules
// Currently empty as all routes have been moved to their respective feature modules

module.exports = router; 